# react-native-permissions-provider

A permissions provider for react-native that handles permission states.

## Installation

```bash
$ npm install --save react-native-permissions-providedr
# --- or ---
$ yarn add react-native-permissions-provider
```
